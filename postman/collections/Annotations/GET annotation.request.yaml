$kind: http-request
name: GET annotation
description: Get a single annotation by ID. 404 if not found.
method: GET
url: '{{baseUrl}}/domain/annotations/{{annotationId}}'
order: 2000
headers:
  - key: Content-Type
    value: application/json
  - key: Cookie
    value: '{{sessionCookie}}'
